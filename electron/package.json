{
  "name": "icore",
  "version": "0.0.5",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build_all_but_image": "cd ../deid && pyinstaller --clean --noconfirm manage.spec && pyinstaller --clean --noconfirm processor.spec && cd ../electron && rm -rf assets/dist && cp ../deid/home/settings.json assets && cp -r ../deid/dist assets && electron-packager . iCore --platform=$(node -p \"process.platform\") --arch=$(node -p \"process.arch\") --icon=assets/icon.icns --overwrite",
    "build_electron": "electron-packager . iCore --platform=$(node -p \"process.platform\") --arch=$(node -p \"process.arch\") --icon=assets/icon.icns --overwrite",
    "build": "cd ../ && docker image rm -f icore_processor || true && docker build . --tag icore_processor && cd electron && docker save -o assets/icore_processor.tar icore_processor && cd ../deid && pyinstaller --clean --noconfirm manage.spec && pyinstaller --clean --noconfirm processor.spec && cd ../electron && rm -rf assets/dist && cp ../deid/home/settings.json assets && cp -r ../deid/dist assets && electron-packager . iCore --platform=$(node -p \"process.platform\") --arch=$(node -p \"process.arch\") --icon=assets/icon.icns --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2"
  }
}
