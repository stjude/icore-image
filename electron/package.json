{
  "name": "icore",
  "version": "0.0.36",
  "main": "main.js",
  "scripts": {
    "start": "ICORE_DEV=1 electron .",
    "build": "electron-builder --mac",
    "build_signed": "CSC_IDENTITY_AUTO_DISCOVERY=true electron-builder --mac",
    "build:win": "electron-builder --win --x64",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "electron": "^42.2.0",
    "electron-builder": "^26.0.12",
    "jest": "^29.7.0"
  },
  "build": {
    "appId": "com.icore.app",
    "productName": "iCore",
    "asar": false,
    "files": [
      "main.js",
      "preload.js",
      "loading.html",
      "lib/**/*",
      "assets/icon.icns",
      "assets/icon.ico",
      "assets/settings.json"
    ],
    "extraResources": [
      {
        "from": "assets/dist",
        "to": "app/assets/dist"
      }
    ],
    "mac": {
      "target": ["dmg", "zip"],
      "icon": "assets/icon.icns",
      "hardenedRuntime": true,
      "entitlements": "entitlements.mac.plist",
      "entitlementsInherit": "entitlements.mac.plist",
      "type": "distribution",
      "publish": {
        "provider": "github",
        "owner": "stjude",
        "repo": "icore-image"
      }
    },
    "win": {
      "target": ["nsis"],
      "icon": "assets/icon.ico",
      "publish": {
        "provider": "github",
        "owner": "stjude",
        "repo": "icore-image"
      }
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "artifactName": "${productName}-Setup-${version}-${arch}.${ext}"
    },
    "dmg": {
      "title": "iCore ${version}",
      "background": "background.png",
      "size": "1g",
      "contents": [
        {
          "x": 150,
          "y": 90,
          "type": "file",
          "name": "iCore.app"
        },
        {
          "x": 150,
          "y": 240,
          "type": "link",
          "path": "/Applications"
        }
      ]
    }
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  }
}
