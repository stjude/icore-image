" Apply auto-formatting on save for Python files
autocmd BufWritePre *.py lua vim.lsp.buf.format({ async = false })

lua << EOF

-- LSP configuration for Python
vim.lsp.enable('ty')
vim.lsp.enable('ruff-lsp')

EOF
